__pycache__/
.pytest_cache/
.claude/
.vscode/
*.pyc
.DS_Store

# Generated codebase overview — regenerate with `make doc`.
project_info.md
project_info__*.md

# Test PDF corpus — fetched on demand with `make test-corpus`.
# (Tarball lives outside the repo; pattern guards against accidental drops.)
test_pdfs/
test_pdfs.tar.gz

# Local tool / editor state, not shared.
.sixth/

# Per-search highlighted PDF copies (regenerated on every search).
.fuzzer_tmp/

# Built C extension (regenerate with `make build-native`).
_ar_norm*.so
native/build/
native/_ar_norm*.so

# HTML docs build output (regenerate with `make docs`).
docs/_build/

# Chat-generated artifacts (Claude's write_file output lands in this folder).
3_Pillars_of_Societal_Peace.html
